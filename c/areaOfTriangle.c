#include <stdio.h>

int main(){
    float base , height;
    printf("Enter the base: ");
    scanf("%i" , &base);
    printf("Enter the height: ");
    scanf("%i" , &height);

    float area;
    printf("The area is %f" , area = height*base*0.5);
}